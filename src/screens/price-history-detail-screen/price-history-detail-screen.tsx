import React, { memo } from 'react';
import { View } from 'react-native';

import { tw } from '../../components';

export const PriceHistoryDetailScreen: React.FC = memo(() => {
  return <View style={tw`flex-1 bg-white`} />;
});
