// import { MutationOptions, useMutation } from 'react-query';
// import messaging from '@react-native-firebase/messaging';

// export const useGetFirebaseDeviceTokenMutation = (options?: MutationOptions<string, unknown, void>) => {
//   return useMutation(() => messaging().getToken(), options);
// };
